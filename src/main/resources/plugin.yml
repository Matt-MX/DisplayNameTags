name: ${name}
main: ${main}
version: ${version}
author: ${author}
api-version: 1.17

depend:
  - packetevents

softdepend:
  - PlaceholderAPI

commands:
  nametags-reload:
    description: Reload the config