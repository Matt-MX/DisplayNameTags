name: ${name}
main: ${main}
version: ${version}
author: ${author}
api-version: 1.17

depend:
  - packetevents

softdepend:
  - PlaceholderAPI
  - TAB

commands:
  nametags-reload:
    description: Reload the config
    permission: nametags.command.reload
